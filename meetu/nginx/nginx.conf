server {
    listen 80;

    # 정적 파일 직접 서빙
    location /static/ {
        alias /staticfiles/;
    }

    # Django로 리버스 프록시
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
